import { postsCollection } from "./content/_schemas/post";
import { projectCollections } from "./content/_schemas/project";

export const collections = {
  posts: postsCollection,
  projects: projectCollections,
};
